name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

env:
  AWS_REGION: us-west-2
  ECR_REPOSITORY: demo-app-repo
  EKS_CLUSTER_NAME: demo-eks-cluster

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    
    - name: Build with Maven
      run: mvn -B package --file pom.xml
    
    - name: Run tests
      run: mvn test
    
    # Temporarily removing the upload-artifact step for initial test
    # - name: Save artifacts
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: app-jar
    #     path: target/*.jar

  # Simplifying the workflow to diagnose issues
  # Commenting out subsequent jobs for now
  # security-scan:
  #   name: Security Scan
  #   needs: build-and-test
  #   runs-on: ubuntu-latest
  #   
  #   steps:
  #   - uses: actions/checkout@v3
  #   
  #   - name: Run OWASP Dependency Check
  #     uses: dependency-check/Dependency-Check_Action@main
  #     with:
  #       project: 'Demo App'
  #       path: '.'
  #       format: 'HTML'
  #       out: 'reports'
  #       
  #   - name: Upload report
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: dependency-check-report
  #       path: reports
  # 
  # build-and-push-image:
  #   ...
  # 
  # deploy-to-kubernetes:
  #   ...